# Copy this file to `.env` and fill in values.
#
# Required for notebooks (Elasticsearch + OpenAI):
# Provide either:
# - ELASTIC_ENDPOINT (preferred in many Elastic Searchlabs examples), or
# - ELASTIC_CLOUD_ID (Elastic Cloud deployments)
ELASTIC_ENDPOINT=
ELASTIC_CLOUD_ID=
ELASTIC_API_KEY=
OPENAI_API_KEY=
#
# Optional: use a LiteLLM proxy instead of calling OpenAI directly:
LITELLM_PROXY_URL=

